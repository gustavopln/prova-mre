version: '3'
services:
  mysql:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_USER: root
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: prova
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - 3307:3306

volumes:
  mysql_data:
